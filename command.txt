#one time each project
git init

#create .gitignore file and add files/folders to be ignored
git add .

#
git status
git config --global user.email "arfahad99@gmail.com"        
git config --global user.name "arfahad99"

git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/arfahad99/knowledge-vault.git
git push -u origin main